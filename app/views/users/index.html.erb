<div class="page-actions">
  <p>
    <%= link_to 'New user', new_user_path, class: "btn" %>
  </p>
</div>

<table class="data-table">
  <thead>
    <tr>
      <th>Name</th>
      <th>Initials</th>
      <th>Roles</th>
      <th>Active</th>
    </tr>
  </thead>
  <tbody>
    <% @users.each do |user| %>
      <tr>
        <td><%= user.name %></td>
        <td><%= user.initials %></td>
        <td><%= user.roles.join(', ') %></td>
        <td><%= render_boolean(user.active?) %></td>
        <td class="action-column">
          <%= link_to '🔎', user_path(user), title: 'View' %>
        </td>
        <td class="action-column">
          <%= link_to '✎', edit_user_path(user), title: 'Edit' %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
<%= render_pagy %>
