services:
  web:
    build: .
    image: manus-web-production:${TAG}
    environment:
      RAILS_MASTER_KEY: cf5d79b9aa08423247034c615fc3b1bb
    ports:
      - "80:3000"
    volumes:
      - db:/rails/storage

volumes:
  db:
