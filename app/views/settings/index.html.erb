<table class="data-table">
  <thead>
    <tr>
      <th>Setting</th>
      <th>Value</th>
    </tr>
  </thead>
  <tbody>
    <% @settings.each do |setting| %>
      <tr>
        <td><%= setting.setting_key %></td>
        <td class="wrappable"><%= setting.setting_value %></td>
        <td class="action-column">
          <%= link_to '✎', edit_setting_path(setting), title: 'Edit' %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
<%= render_pagy %>
